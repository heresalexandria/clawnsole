bool get shellManagesDataLocation => false;

Future<Map<String, Object?>> revealShellDataFolder() => throw StateError(
  'The data folder is managed by the packaged Clawnsole desktop app.',
);

Future<Map<String, Object?>> chooseShellDataDirectory() => throw StateError(
  'The data folder is managed by the packaged Clawnsole desktop app.',
);
